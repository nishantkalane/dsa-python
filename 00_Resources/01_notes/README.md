# 📚 General Notes

Cross-topic theory, cheat-sheets, complexity tables, Python-specific
tricks (e.g. `heapq`, `collections.deque`, `bisect`), and anything
that doesn't belong to a single topic folder.
